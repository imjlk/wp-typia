## PR2 설계 (PR1 머지 후 진행)

### 기본 maxDepth = 5
2~4뎁스 실사용 케이스에 여유를 둔 값. manifest overhead ~3.4KB로 관리 가능.

### PR1 머지 우선
PR1(#1156)의 리뷰가 완료되었으므로 먼저 머지하고, 그 후 PR2를 진행.

### PR2 구현 요약 (변경된 기본값 반영)
1. `AnalysisContext.recursionGuard`: `Set<string>` → `Map<string, number>` depth counter
2. `AnalysisContext.maxRecursiveDepth: number` 추가 (기본값 **5**)
3. `parseNamedDeclaration` guard 로직: throw → depth 체크 후 terminal node(empty object, required: false) 반환
4. `analyzeSourceType`/`analyzeSourceTypes` options에 `maxRecursiveDepth?: number` 추가
5. 기존 recursive-type 테스트 업데이트 +展開 테스트 추가
6. changeset: `@wp-typia/block-runtime` minor